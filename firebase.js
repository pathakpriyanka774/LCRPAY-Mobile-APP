// firebase.js - Firebase initialization
// For React Native Firebase, we don't need manual initialization
// The native modules handle this automatically via google-services.json

// Just export an empty object since React Native Firebase
// initializes automatically from the native configuration files
export default {};